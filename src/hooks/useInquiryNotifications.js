// Removed: Java SSE inquiry stream was redundant.
// Inquiry notifications are handled by ChatContext via the WebSocket connection.
// eslint-disable-next-line no-unused-vars
const useInquiryNotifications = (_isAuthed) => {};
export default useInquiryNotifications;
