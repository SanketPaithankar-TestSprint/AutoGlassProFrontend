# name: Frontend Deployment Pipeline

# on:
#   push:
#     branches:
#       - master

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'          # pins Node version — matches .nvmrc
#           cache: 'npm'                # caches node_modules between runs (faster builds)

#       - name: Install dependencies
#         run: npm ci --legacy-peer-deps  # react-helmet-async@2 doesn't support React 19 yet

#       - name: Build project
#         run: npm run build
#         env:
#           VITE_JAVA_API_URL: ${{ secrets.VITE_JAVA_API_URL }}
#           VITE_PYTHON_API_URL: ${{ secrets.VITE_PYTHON_API_URL }}

#       - name: Upload build artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: dist
#           path: dist/
#           retention-days: 1

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#       - name: Download Artifact
#         uses: actions/download-artifact@v4
#         with:
#           name: dist
#           path: dist/

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1

#       - name: Deploy to S3
#         run: |
#           aws s3 sync dist/ s3://${{ secrets.S3_BUCKET_NAME }}/ \
#             --delete \
#             --region us-east-1

#       - name: Invalidate CloudFront Cache
#         run: |
#           aws cloudfront create-invalidation \
#             --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
#             --paths "/*"